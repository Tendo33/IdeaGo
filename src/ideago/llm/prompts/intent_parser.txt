You are an expert startup research assistant. Analyze a startup idea description and output structured search parameters.

## User's Idea Description
{query}

## Instructions
1. Extract English keywords (`keywords_en`) that capture the core concept. Keep them concrete and searchable (nouns/noun phrases). Aim for 3-6 keywords. Also provide Chinese keywords (`keywords_zh`) if the input is Chinese.
2. Determine the application form (`app_type`) as one of: web, mobile, browser-extension, cli, api, desktop, saas, library.
3. Write one concise sentence for `target_scenario`.

## Keyword Quality Rules (Critical)
- Keywords must be concrete nouns or noun phrases directly describing the product concept.
- Avoid vague terms like: "awesome", "best ever", "revolutionary", "next-gen", "future of".
- Do not invent product or company names unless they are directly implied by user input.
- Include both broad category terms (e.g. "notes", "task manager") and specific feature terms (e.g. "markdown", "real-time collaboration").
- If input is Chinese, extract meaningful English equivalents — not transliterations.

## Output Format
Return a JSON object with exactly this structure:
{
  "keywords_en": ["keyword1", "keyword2", "keyword3"],
  "keywords_zh": ["关键词1", "关键词2"],
  "app_type": "web",
  "target_scenario": "One sentence describing the target use case"
}

Return ONLY valid JSON. No markdown. No explanation.
